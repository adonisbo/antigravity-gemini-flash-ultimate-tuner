# Antigravity Ultimate Tuner Rules - v2026.03.18
#
# ╔══════════════════════════════════════════════════════════════╗
# ║  🌐 语言开关（Language Switch）                              ║
# ║  修改第 5 条规则即可切换语言，只需改一处：                     ║
# ║  简体中文（默认）→ "语言：所有回复和代码注释必须使用简体中文。"║
# ║  English         → "Language: All responses in English."    ║
# ║  繁體中文        → "語言：所有回覆必須使用繁體中文。"          ║
# ╚══════════════════════════════════════════════════════════════╝
#
# ╔══════════════════════════════════════════════════════════════╗
# ║  🤖 模型档位开关（Model Tier Switch）                        ║
# ║                                                              ║
# ║  本规则文件针对 Antigravity 中的所有模型自动加载。            ║
# ║  请根据你当前选择的模型，启用对应档位（取消注释对应行）：      ║
# ║                                                              ║
# ║  【轻量模型档】适用模型：Gemini 3 Flash、GPT-OSS 120B        ║
# ║    → 取消注释下方 LITE MODE 区块（默认已启用）               ║
# ║                                                              ║
# ║  【强力模型档】适用模型：Gemini 3.1 Pro、Claude Opus 4.6     ║
# ║    → 取消注释下方 PRO MODE 区块，并注释掉 LITE MODE 区块     ║
# ║                                                              ║
# ║  【通用模型档】适用模型：Claude Sonnet 4.6                   ║
# ║    → 取消注释下方 STANDARD MODE 区块                        ║
# ╚══════════════════════════════════════════════════════════════╝
#
# 注意：本文件由 Antigravity 对所有对话自动加载（无论选择何种模型）
# Skills 是按需加载的，不受模型限制影响。
# 如遇 Gemini CLI 覆盖全局 Rules，请运行 tools/conflict_check.bat 检测。

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 【LITE MODE】轻量模型档 - 适用：Gemini 3 Flash / GPT-OSS 120B
# 默认启用。使用强力模型时请注释掉此区块。
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

任何任务必须严格遵守以下流程，不允许任何跳过或例外：

1. **第一步**：必须调用 Best-Practice-Researcher（至少 2 次真实 Browser/search_web 调用，并显示 query + 关键摘要）。禁止仅凭内部知识。

2. **第二步**：如果涉及架构/规划，必须调用 Architect-Dialogue 输出方案对比表 + 树状图 + 任务拆分 + 用户确认。

3. **错误处理**：遇到任何错误、警告、潜在问题，自动进入 reflection 循环（完整日志分析 + 至少 2 个修复方案 + 测试验证步骤）。绝不允许说"模型限制"或直接放弃。

4. **输出规范**：所有输出必须包含工具调用证据（query + 摘要）。禁止 hallucination。

5. **语言**：所有回复和代码注释必须使用简体中文。
   # ↑↑↑ 语言开关：如需英文，改为 "Language: All responses and code comments in English."

6. **优先级顺序**：Researcher → Architect-Dialogue → 编码/调试 → 测试验证。

7. **完整任务约束**：在任何涉及编码、架构、调试、优化的任务中，必须：
   - 第一步：调用 Best-Practice-Researcher 进行真实搜索（至少 2 次工具调用，显示原始查询和关键摘要）
   - 第二步：如果涉及规划，必须调用 Architect-Dialogue 输出方案对比、树状图、任务列表，等待用户明确确认
   - 遇到任何错误，自动进入 reflection 循环：完整日志分析 + 至少 2 个修复方案 + 测试验证步骤
   - 所有输出必须包含工具调用证据，不允许仅凭内部知识生成

8. **Skills 优先**：任何任务必须优先检查并调用相关 Skill，严格按 Skill 定义的流程执行，不允许跳过。

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 【STANDARD MODE】通用档 - 适用：Claude Sonnet 4.6
# 默认已注释。使用 Sonnet 时取消注释此区块，同时注释 LITE MODE。
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. 遇到版本敏感的技术选型（库、框架、部署方案），建议先调用 Best-Practice-Researcher 确认最新版本，避免使用过时方案。
# 2. 涉及大型架构决策时，建议调用 Architect-Dialogue 做方案对比，等待确认后再执行。
# 3. 错误处理：遇到报错自动给出日志分析和至少 2 个修复方案。
# 4. 语言：所有回复和代码注释必须使用简体中文。

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 【PRO MODE】强力模型档 - 适用：Gemini 3.1 Pro / Claude Opus 4.6
# 默认已注释。使用强力模型时取消注释此区块，同时注释 LITE MODE。
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. 语言：所有回复和代码注释必须使用简体中文。
# 2. 涉及重大架构决策或版本选型时，可自行判断是否需要联网确认，无需强制执行。
# 3. 遇到错误时，自主分析并给出最佳修复方案，无需强制 reflection 流程。
# 4. Skills 作为参考工具，按需自主调用，无需强制触发。

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 【通用规则 - 所有模型始终生效】
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- **冲突保护**：如果检测到全局 `~/.gemini/GEMINI.md` 被其他工具覆盖，自动提示用户优先使用项目级 Rules，并告知运行 tools/conflict_check.bat 进行检测。
